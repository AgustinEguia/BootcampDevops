¡Listo! El chart "{{ .Chart.Name }}" (release "{{ .Release.Name }}") se instaló
en el namespace "{{ .Release.Namespace }}".

Para ver el estado del deployment:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if .Values.ingress.enabled }}

La app debería quedar accesible (si el Ingress Controller y el DNS/hosts
están configurados) en:
  http://{{ .Values.ingress.host }}
{{- else }}

El Ingress está deshabilitado (ingress.enabled=false). Para acceder a la
app, podés hacer port-forward directo al Service:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "demo-app.fullname" . }} 8000:{{ .Values.service.port }}
  # y después abrir http://localhost:8000
{{- end }}

Endpoints útiles una vez que tengas acceso:
  GET /            -> saludo + versión desplegada
  GET /healthz      -> liveness
  GET /readyz        -> readiness
  GET /metrics         -> métricas Prometheus
  GET /api/tasks         -> CRUD de tareas (en memoria)

{{- if .Values.autoscaling.enabled }}

Autoscaling HABILITADO: entre {{ .Values.autoscaling.minReplicas }} y
{{ .Values.autoscaling.maxReplicas }} réplicas, según uso de CPU
(objetivo: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}%).
Para verlo en acción:
  kubectl get hpa -n {{ .Release.Namespace }} -w
{{- end }}

Para ver los últimos releases (útil para un rollback):
  helm history {{ .Release.Name }} -n {{ .Release.Namespace }}
